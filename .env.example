# Shopify CLI environment
# Set this to your development store domain
SHOPIFY_FLAG_STORE=your-dev-store.myshopify.com

# Optional: Theme access token (not required if you use `shopify auth login`)
SHOPIFY_CLI_THEME_TOKEN=
